{
  "_comment": "Copy to secrets.json (gitignored). Off-chain secrets the CRE workflow needs. In MOCK / local-simulation against anvil none of these are required; leave them blank. For a live volatile-token deploy, fill the Data Streams creds so the workflow can fetch signed reports via the HTTP capability.",
  "CHAINLINK_DATASTREAMS_API_KEY": "",
  "CHAINLINK_DATASTREAMS_API_SECRET": ""
}
