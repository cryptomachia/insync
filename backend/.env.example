# Handoff backend env (AGENT 9). Runs in MOCK/local mode with no third-party keys.
MOCK=true

# Chain / indexer. Leave ESCROW_ADDRESS blank to run API-only (no indexer).
RPC_URL=http://127.0.0.1:8545
ESCROW_ADDRESS=

# API
PORT=8787
DATABASE_PATH=./handoff.db
